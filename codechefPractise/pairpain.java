/* package codechef; // don't place package name! */

import java.util.*;
import java.lang.*;
import java.io.*;

/* Name of the class has to be "Main" only if the class is public. */
class Codechef
{
	public static void main (String[] args) throws java.lang.Exception
	{

    // observations = if any pairs which includes 1 then condition is satisfying
    // other then 1 is 2 if anything which have more than 2 then it false the condition 
    // egg 2 + 2 = 2 * 2 but 2 + 3 != 2 * 3 

	  Scanner sc = new Scanner(System.in);
		int t = sc.nextInt();
		while(t-->0){
		   int n = sc.nextInt();
		   int[] arr = new int[n];
		   int c = 0;
		   int one = 0 , two = 0;
		   int ip = sc.nextInt();
		    for(int i = 0; i < arr.length;i++){
		      arr[i] = sc.nextInt();
		    }
       
         c1*(n - 1)+ c1 * (c1 - 1)/2 + (c2 - 1)/2
        System.out.println(c+(two*(two+1)/2));
		}
		}
}
